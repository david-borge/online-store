import { ImageIsLoadedDirective } from './image-is-loaded.directive';

describe('ImageIsLoadedDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageIsLoadedDirective();
    expect(directive).toBeTruthy();
  });
});
